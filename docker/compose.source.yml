version: "3"
services:
  nas-tools:
    build:
      # 构建上下文为仓库根目录，请在仓库根目录使用
      #   docker compose -f docker/compose.source.yml up -d --build
      context: ..
      dockerfile: docker/Dockerfile.source
      args:
        - PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple   # 国内网络加速，海外可注释本行
        #- ALPINE_MIRROR=mirrors.ustc.edu.cn                      # 国内网络加速，海外可注释本行
    image: nas-tools:source
    ports:
      - 3000:3000        # 默认的webui控制端口
    volumes:
      - ./config:/config   # 冒号左边请修改为你想保存配置的路径
      - /你的媒体目录:/你想设置的容器内能见到的目录   # 媒体目录，多个目录需要分别映射进来，需要满足配置文件说明中的要求
    environment:
      - PUID=0    # 想切换为哪个用户来运行程序，该用户的uid
      - PGID=0    # 想切换为哪个用户来运行程序，该用户的gid
      - UMASK=000 # 掩码权限，默认000，可以考虑设置为022
      - NASTOOL_AUTO_UPDATE=false  # 源码构建的镜像不含.git，自动更新不可用，请保持false，更新请重新构建镜像
    restart: always
    network_mode: bridge
    hostname: nas-tools
    container_name: nas-tools
